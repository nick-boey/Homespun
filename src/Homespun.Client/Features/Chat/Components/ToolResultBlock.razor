<div class="bg-[var(--bg-tertiary)] p-1 rounded-sm font-mono text-sm">
    <pre class="m-0 p-1 bg-[var(--bg-tool-group)] rounded-sm overflow-x-auto whitespace-pre-wrap break-words max-h-[200px] overflow-y-auto">@Text</pre>
</div>

@code {
    [Parameter]
    public string? Text { get; set; }
}
