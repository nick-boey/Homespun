@* Consolidated showcase for chat-related components: ChatInput, ChatMessage, ContentBlock, TextBlock, ThinkingBlock, ToolUseBlock, ToolResultBlock, SessionHistoryList, and SessionsPanel *@

<h2>ChatInput</h2>

<div class="showcase-section">
    <h3>Default State</h3>
    <div class="showcase-item">
        <div class="showcase-label">Empty input ready for typing</div>
        <div class="showcase-preview">
            <ChatInput
                Value="@_value1"
                ValueChanged="@(v => _value1 = v)"
                OnSend="@(msg => _lastSent1 = msg)" />
            @if (!string.IsNullOrEmpty(_lastSent1))
            {
                <p class="text-text-muted text-sm mt-2">Last sent: "@_lastSent1"</p>
            }
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>With Content</h3>
    <div class="showcase-item">
        <div class="showcase-label">Input with pre-filled text</div>
        <div class="showcase-preview">
            <ChatInput
                Value="@_value2"
                ValueChanged="@(v => _value2 = v)"
                OnSend="@(msg => _lastSent2 = msg)" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Custom Placeholder</h3>
    <div class="showcase-item">
        <div class="showcase-label">Input with custom placeholder text</div>
        <div class="showcase-preview">
            <ChatInput
                Value="@_value3"
                ValueChanged="@(v => _value3 = v)"
                Placeholder="Describe the feature you want to implement..."
                OnSend="@(msg => _lastSent3 = msg)" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Disabled State</h3>
    <div class="showcase-item">
        <div class="showcase-label">Input disabled while processing</div>
        <div class="showcase-preview">
            <ChatInput
                Value="@_value4"
                ValueChanged="@(v => _value4 = v)"
                Disabled="true"
                Placeholder="Waiting for response..."
                OnSend="@(msg => { })" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>In Context</h3>
    <div class="showcase-item">
        <div class="showcase-label">Input within a chat interface context</div>
        <div class="showcase-preview">
            <div class="chat-context-preview">
                <div class="chat-messages-area">
                    <div class="text-text-muted text-sm text-center py-4">
                        Chat messages would appear here...
                    </div>
                </div>
                <div class="chat-input-wrapper">
                    <ChatInput
                        Value="@_value5"
                        ValueChanged="@(v => _value5 = v)"
                        OnSend="@(msg => _lastSent5 = msg)" />
                </div>
            </div>
            @if (!string.IsNullOrEmpty(_lastSent5))
            {
                <p class="text-text-muted text-sm mt-2">Last sent: "@_lastSent5"</p>
            }
        </div>
    </div>
</div>

<h2>ChatMessage</h2>

<div class="showcase-section">
    <h3>User Message</h3>
    <div class="showcase-item">
        <div class="showcase-label">Message from the user</div>
        <div class="showcase-preview">
            <ChatMessage Message="@_userMessage" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Assistant Text Message</h3>
    <div class="showcase-item">
        <div class="showcase-label">Simple text response from Claude</div>
        <div class="showcase-preview">
            <ChatMessage Message="@_assistantTextMessage" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Message with Tool Use</h3>
    <div class="showcase-item">
        <div class="showcase-label">Claude invoking a tool</div>
        <div class="showcase-preview">
            <ChatMessage Message="@_toolUseMessage" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Message with Tool Result</h3>
    <div class="showcase-item">
        <div class="showcase-label">Result from a tool invocation</div>
        <div class="showcase-preview">
            <ChatMessage Message="@_toolResultMessage" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Message with Thinking</h3>
    <div class="showcase-item">
        <div class="showcase-label">Claude's thinking process</div>
        <div class="showcase-preview">
            <ChatMessage Message="@_thinkingMessage" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Conversation Flow</h3>
    <div class="showcase-item">
        <div class="showcase-label">Multiple messages in sequence</div>
        <div class="showcase-preview">
            <ChatMessage Message="@_conversationUser" />
            <ChatMessage Message="@_conversationAssistant" />
        </div>
    </div>
</div>

<h2>ContentBlock</h2>

<div class="showcase-section">
    <h3>Text Content Block</h3>
    <div class="showcase-item">
        <div class="showcase-label">Renders text with the TextBlock component</div>
        <div class="showcase-preview">
            <ContentBlock Content="_textContent" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Thinking Content Block</h3>
    <div class="showcase-item">
        <div class="showcase-label">Renders thinking with the ThinkingBlock component</div>
        <div class="showcase-preview">
            <ContentBlock Content="_thinkingContent" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Tool Use Content Block</h3>
    <div class="showcase-item">
        <div class="showcase-label">Renders tool invocation with the ToolUseBlock component</div>
        <div class="showcase-preview">
            <ContentBlock Content="_toolUseContent" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Tool Result Content Block</h3>
    <div class="showcase-item">
        <div class="showcase-label">Renders tool result with the ToolResultBlock component</div>
        <div class="showcase-preview">
            <ContentBlock Content="_toolResultContent" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Streaming Content Block</h3>
    <div class="showcase-item">
        <div class="showcase-label">Content currently being streamed</div>
        <div class="showcase-preview">
            <ContentBlock Content="_streamingContent" />
        </div>
    </div>
</div>

<h2>TextBlock</h2>

<div class="showcase-section">
    <h3>Simple Text</h3>
    <div class="showcase-item">
        <div class="showcase-label">Basic text content</div>
        <div class="showcase-preview">
            <TextBlock Text="This is a simple text message from the assistant." />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Multi-line Text</h3>
    <div class="showcase-item">
        <div class="showcase-label">Text with line breaks</div>
        <div class="showcase-preview">
            <TextBlock Text="@_multilineText" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Streaming State</h3>
    <div class="showcase-item">
        <div class="showcase-label">Text currently being streamed (shows cursor)</div>
        <div class="showcase-preview">
            <TextBlock Text="I'm analyzing your codebase and will provide recommendations" IsStreaming="true" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Long Text</h3>
    <div class="showcase-item">
        <div class="showcase-label">Extended text content</div>
        <div class="showcase-preview">
            <TextBlock Text="@_longText" />
        </div>
    </div>
</div>

<h2>ThinkingBlock</h2>

<div class="showcase-section">
    <h3>Collapsed Thinking</h3>
    <div class="showcase-item">
        <div class="showcase-label">Completed thinking process (expandable)</div>
        <div class="showcase-preview">
            <ThinkingBlock Text="Looking at the existing auth service, I can see it expects email and password parameters. The login function makes a POST request to /api/auth/login. I should create a form that validates the email format using a regex pattern and ensures the password meets minimum length requirements before calling the login function." />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Streaming Thinking</h3>
    <div class="showcase-item">
        <div class="showcase-label">Claude currently thinking (expanded by default)</div>
        <div class="showcase-preview">
            <ThinkingBlock
                Text="Analyzing the codebase structure to understand the authentication flow"
                IsStreaming="true" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Long Thinking Process</h3>
    <div class="showcase-item">
        <div class="showcase-label">Extended reasoning with multiple considerations</div>
        <div class="showcase-preview">
            <ThinkingBlock Text="@_longThinking" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Short Thinking</h3>
    <div class="showcase-item">
        <div class="showcase-label">Brief consideration</div>
        <div class="showcase-preview">
            <ThinkingBlock Text="This is a simple change - I just need to update the button text." />
        </div>
    </div>
</div>

<h2>ToolUseBlock</h2>

<div class="showcase-section">
    <h3>Read Tool</h3>
    <div class="showcase-item">
        <div class="showcase-label">Reading a file</div>
        <div class="showcase-preview">
            <ToolUseBlock
                ToolName="Read"
                ToolInput="@_readInput"/>
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Edit Tool</h3>
    <div class="showcase-item">
        <div class="showcase-label">Editing a file</div>
        <div class="showcase-preview">
            <ToolUseBlock
                ToolName="Edit"
                ToolInput="@_editInput"/>
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Bash Tool</h3>
    <div class="showcase-item">
        <div class="showcase-label">Running a command</div>
        <div class="showcase-preview">
            <ToolUseBlock
                ToolName="Bash"
                ToolInput="@_bashInput"/>
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Streaming Tool Use</h3>
    <div class="showcase-item">
        <div class="showcase-label">Tool currently executing (shows spinner)</div>
        <div class="showcase-preview">
            <ToolUseBlock
                ToolName="Glob"
                ToolInput="@_globInput"
                IsStreaming="true"/>
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Complex Tool Input</h3>
    <div class="showcase-item">
        <div class="showcase-label">Tool with longer input</div>
        <div class="showcase-preview">
            <ToolUseBlock
                ToolName="Write"
                ToolInput="@_complexInput"/>
        </div>
    </div>
</div>

<h2>ToolResultBlock</h2>

<div class="showcase-section">
    <h3>File Content Result</h3>
    <div class="showcase-item">
        <div class="showcase-label">Result from reading a file</div>
        <div class="showcase-preview">
            <ToolResultBlock Text="@_fileContent" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Command Output</h3>
    <div class="showcase-item">
        <div class="showcase-label">Result from a bash command</div>
        <div class="showcase-preview">
            <ToolResultBlock Text="@_commandOutput" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Short Result</h3>
    <div class="showcase-item">
        <div class="showcase-label">Simple success message</div>
        <div class="showcase-preview">
            <ToolResultBlock Text="File successfully written to /src/components/Button.tsx" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Error Result</h3>
    <div class="showcase-item">
        <div class="showcase-label">Error output from a failed command</div>
        <div class="showcase-preview">
            <ToolResultBlock Text="@_errorOutput" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Search Results</h3>
    <div class="showcase-item">
        <div class="showcase-label">Result from a grep/search operation</div>
        <div class="showcase-preview">
            <ToolResultBlock Text="@_searchResults" />
        </div>
    </div>
</div>

<h2>SessionHistoryList</h2>

@*
    SessionHistoryList depends on IClaudeSessionApi.
    This showcase renders a static mock version of the component.
*@

<div class="showcase-section">
    <h3>With Sessions</h3>
    <div class="showcase-item">
        <div class="showcase-label">List of resumable sessions</div>
        <div class="showcase-preview">
            <MockSessionHistoryList Sessions="_sessions" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Empty State</h3>
    <div class="showcase-item">
        <div class="showcase-label">No previous sessions</div>
        <div class="showcase-preview">
            <MockSessionHistoryList Sessions="[]" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Loading State</h3>
    <div class="showcase-item">
        <div class="showcase-label">Sessions being loaded</div>
        <div class="showcase-preview">
            <MockSessionHistoryList IsLoading="true" />
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Resuming Session</h3>
    <div class="showcase-item">
        <div class="showcase-label">Session being resumed</div>
        <div class="showcase-preview">
            <MockSessionHistoryList Sessions="_sessions" ResumingSessionId="session-2" />
        </div>
    </div>
</div>

<h2>SessionsPanel</h2>

@*
    SessionsPanel depends on SignalR and session services.
    This showcase provides a description of its features.
*@

<div class="showcase-section">
    <h3>Component Overview</h3>
    <div class="showcase-item">
        <div class="showcase-preview">
            <div class="alert alert-info mb-0">
                <strong>SessionsPanel</strong> is a unified panel for managing Claude Code agent sessions.
                It displays session information with real-time updates via SignalR.
            </div>
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Key Features</h3>
    <div class="showcase-item">
        <div class="showcase-preview">
            <ul class="list-unstyled mb-0">
                <li class="mb-2">
                    <span class="badge bg-primary me-2">Unified</span>
                    Single component for both global and project-specific views
                </li>
                <li class="mb-2">
                    <span class="badge bg-info me-2">Grouping</span>
                    Groups by project (global) or by status (project-specific)
                </li>
                <li class="mb-2">
                    <span class="badge bg-success me-2">Real-time</span>
                    SignalR integration for live status updates
                </li>
                <li class="mb-2">
                    <span class="badge bg-danger me-2">Stop</span>
                    Ability to stop running sessions
                </li>
                <li class="mb-2">
                    <span class="badge bg-secondary me-2">Navigate</span>
                    Quick access to session chat interface
                </li>
            </ul>
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>View Modes</h3>
    <div class="showcase-item">
        <div class="showcase-label">Global View (no ProjectId parameter)</div>
        <div class="showcase-preview">
            <div class="card" style="max-width: 500px;">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <span>Project Alpha</span>
                    <span class="badge bg-secondary">2 sessions</span>
                </div>
                <div class="card-body">
                    <p class="text-text-muted text-sm mb-0">
                        Sessions are grouped by project name and sorted by status priority within each group.
                    </p>
                </div>
            </div>
        </div>
    </div>

    <div class="showcase-item">
        <div class="showcase-label">Project View (with ProjectId parameter)</div>
        <div class="showcase-preview">
            <div class="card" style="max-width: 500px;">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <div class="d-flex align-items-center gap-2">
                        <span class="status-dot bg-success"></span>
                        <span>Working</span>
                    </div>
                    <span class="badge bg-secondary">3 sessions</span>
                </div>
                <div class="card-body">
                    <p class="text-text-muted text-sm mb-0">
                        Sessions are grouped by status (Plan Ready, Question, Waiting, Working, etc.) and sorted by recent activity.
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Session Card States</h3>
    <div class="showcase-item">
        <div class="showcase-label">Session card with full details</div>
        <div class="showcase-preview">
            <div class="card" style="max-width: 400px;">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <div class="d-flex align-items-center gap-2">
                        <span class="badge bg-warning text-dark">Issue</span>
                        <span class="fw-medium">Fix authentication bug</span>
                    </div>
                    <span class="status-dot bg-success" style="width: 10px; height: 10px; border-radius: 50%;"></span>
                </div>
                <div class="card-body">
                    <div class="mb-2 small">
                        <span class="text-text-muted">Mode:</span>
                        <span class="badge bg-success ms-1">Build</span>
                    </div>
                    <div class="mb-2 small">
                        <span class="text-text-muted">Status:</span>
                        <span class="badge bg-success ms-1">Working</span>
                    </div>
                    <div class="mb-2 small">
                        <span class="text-text-muted">Started:</span>
                        <span class="ms-1">15m 30s ago</span>
                    </div>
                    <div class="mb-2 small">
                        <span class="text-text-muted">Messages:</span>
                        <span class="ms-1">42</span>
                    </div>
                </div>
                <div class="card-footer d-flex gap-2">
                    <a href="#" class="btn btn-sm btn-outline-primary">
                        <LucideIcon Name="message-circle" Size="14" Class="me-1" />Chat
                    </a>
                    <button class="btn btn-sm btn-outline-danger">
                        <LucideIcon Name="stop-circle" Size="14" Class="me-1" />Stop
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="showcase-section">
    <h3>Status Priority Order</h3>
    <div class="showcase-item">
        <div class="showcase-preview">
            <ol class="mb-0 small">
                <li><strong>Plan Ready</strong> - Requires user approval to proceed</li>
                <li><strong>Question</strong> - Waiting for user to answer a question</li>
                <li><strong>Waiting</strong> - Ready for user input</li>
                <li><strong>Working</strong> - Actively processing</li>
                <li><strong>Starting</strong> - Session initializing</li>
                <li><strong>Running Hooks</strong> - Executing pre/post hooks</li>
                <li><strong>Stopped</strong> - Session terminated</li>
                <li><strong>Error</strong> - Session encountered an error</li>
            </ol>
        </div>
    </div>
</div>

<style>
    .chat-context-preview {
        border: 1px solid var(--border-color);
        border-radius: var(--radius-md);
        overflow: hidden;
    }

    .chat-messages-area {
        background: var(--bg-secondary);
        min-height: 150px;
        padding: var(--spacing-md);
    }

    .chat-input-wrapper {
        border-top: 1px solid var(--border-color);
        background: var(--bg-primary);
    }

    .status-dot {
        width: 10px;
        height: 10px;
        border-radius: 50%;
        display: inline-block;
    }
</style>

@code {
    // ChatInput
    private string _value1 = "";
    private string _value2 = "Can you help me fix the failing tests?";
    private string _value3 = "";
    private string _value4 = "Processing your request...";
    private string _value5 = "";

    private string _lastSent1 = "";
    private string _lastSent2 = "";
    private string _lastSent3 = "";
    private string _lastSent5 = "";

    // ChatMessage
    private ClaudeMessage _userMessage = new()
    {
        SessionId = "demo",
        Role = ClaudeMessageRole.User,
        Content = new()
        {
            new ClaudeMessageContent
            {
                Type = ClaudeContentType.Text,
                Text = "Can you help me implement a login form with email and password validation?"
            }
        },
        CreatedAt = DateTime.UtcNow.AddMinutes(-5)
    };

    private ClaudeMessage _assistantTextMessage = new()
    {
        SessionId = "demo",
        Role = ClaudeMessageRole.Assistant,
        Content = new()
        {
            new ClaudeMessageContent
            {
                Type = ClaudeContentType.Text,
                Text = "I'd be happy to help you implement a login form! Let me create a form component with proper email and password validation.\n\nHere's what I'll include:\n- Email format validation\n- Password minimum length check\n- Error message display\n- Submit handling"
            }
        },
        CreatedAt = DateTime.UtcNow.AddMinutes(-4)
    };

    private ClaudeMessage _toolUseMessage = new()
    {
        SessionId = "demo",
        Role = ClaudeMessageRole.Assistant,
        Content = new()
        {
            new ClaudeMessageContent
            {
                Type = ClaudeContentType.Text,
                Text = "Let me read the existing authentication service to understand the API:"
            },
            new ClaudeMessageContent
            {
                Type = ClaudeContentType.ToolUse,
                ToolName = "Read",
                ToolInput = """{"file_path": "/src/services/auth.ts"}""",
                ToolUseId = "tool_01"
            }
        },
        CreatedAt = DateTime.UtcNow.AddMinutes(-3)
    };

    private ClaudeMessage _toolResultMessage = new()
    {
        SessionId = "demo",
        Role = ClaudeMessageRole.User,
        Content = new()
        {
            new ClaudeMessageContent
            {
                Type = ClaudeContentType.ToolResult,
                ToolUseId = "tool_01",
                ToolResult = "export const login = async (email: string, password: string) => {\n  const response = await fetch('/api/auth/login', {\n    method: 'POST',\n    body: JSON.stringify({ email, password })\n  });\n  return response.json();\n};",
                ToolSuccess = true
            }
        },
        CreatedAt = DateTime.UtcNow.AddMinutes(-2)
    };

    private ClaudeMessage _thinkingMessage = new()
    {
        SessionId = "demo",
        Role = ClaudeMessageRole.Assistant,
        Content = new()
        {
            new ClaudeMessageContent
            {
                Type = ClaudeContentType.Thinking,
                Thinking = "Looking at the existing auth service, I can see it expects email and password parameters. I should create a form that validates the email format using a regex pattern and ensures the password meets minimum length requirements before calling the login function."
            },
            new ClaudeMessageContent
            {
                Type = ClaudeContentType.Text,
                Text = "I've analyzed the authentication service and I'll now create the login form component."
            }
        },
        CreatedAt = DateTime.UtcNow.AddMinutes(-1)
    };

    private ClaudeMessage _conversationUser = new()
    {
        SessionId = "demo",
        Role = ClaudeMessageRole.User,
        Content = new()
        {
            new ClaudeMessageContent
            {
                Type = ClaudeContentType.Text,
                Text = "Add a \"Remember me\" checkbox to the login form"
            }
        },
        CreatedAt = DateTime.UtcNow.AddSeconds(-30)
    };

    private ClaudeMessage _conversationAssistant = new()
    {
        SessionId = "demo",
        Role = ClaudeMessageRole.Assistant,
        Content = new()
        {
            new ClaudeMessageContent
            {
                Type = ClaudeContentType.Text,
                Text = "I'll add a \"Remember me\" checkbox that stores the user's preference in local storage. This will keep the user logged in across browser sessions."
            }
        },
        CreatedAt = DateTime.UtcNow
    };

    // ContentBlock
    private ClaudeMessageContent _textContent = new()
    {
        Type = ClaudeContentType.Text,
        Text = "I'll help you implement the login form with validation."
    };

    private ClaudeMessageContent _thinkingContent = new()
    {
        Type = ClaudeContentType.Thinking,
        Text = "I need to check the existing form structure before making changes.",
        Thinking = "I need to check the existing form structure before making changes."
    };

    private ClaudeMessageContent _toolUseContent = new()
    {
        Type = ClaudeContentType.ToolUse,
        ToolName = "Read",
        ToolInput = """{"file_path": "/src/components/LoginForm.tsx"}""",
        ToolUseId = "tool_123"
    };

    private ClaudeMessageContent _toolResultContent = new()
    {
        Type = ClaudeContentType.ToolResult,
        Text = "export const LoginForm = () => { return <form>...</form>; }",
        ToolUseId = "tool_123",
        ToolResult = "export const LoginForm = () => { return <form>...</form>; }",
        ToolSuccess = true
    };

    private ClaudeMessageContent _streamingContent = new()
    {
        Type = ClaudeContentType.Text,
        Text = "Analyzing the current implementation",
        IsStreaming = true
    };

    // TextBlock
    private string _multilineText = """
        Here's what I found in your codebase:

        1. The authentication module is well-structured
        2. There are some unused imports in the utils folder
        3. The API endpoints follow RESTful conventions

        Let me know if you'd like me to address any of these.
        """;

    private string _longText = "I've analyzed the entire codebase and found several opportunities for improvement. The main areas include code organization, where some modules could benefit from better separation of concerns; performance optimization, particularly in the data fetching layer where we could implement caching; and test coverage, which is currently at 65% but could be improved to meet the 80% target. I recommend starting with the authentication module as it's the most critical component and has the least coverage.";

    // ThinkingBlock
    private string _longThinking = """
        Let me analyze the requirements for this feature:

        1. First, I need to understand the current authentication flow. The app uses JWT tokens stored in localStorage, with refresh tokens handled server-side.

        2. For the "Remember me" checkbox:
           - If checked: Set token expiry to 30 days
           - If unchecked: Set token expiry to session only

        3. I should also consider:
           - Security implications of longer token lifetimes
           - The UX of the checkbox placement in the form
           - Whether to persist the checkbox preference itself

        4. Implementation approach:
           - Add a boolean parameter to the login API
           - Store the user preference in localStorage
           - Update the token refresh logic accordingly

        I'll start by modifying the login form component, then update the auth service.
        """;

    // ToolUseBlock
    private string _readInput = """{"file_path": "/src/services/auth.ts"}""";

    private string _editInput = """{"file_path": "/src/components/Button.tsx", "old_string": "onClick={() => {}", "new_string": "onClick={handleClick}"}""";

    private string _bashInput = """{"command": "npm run test -- --coverage"}""";

    private string _globInput = """{"pattern": "**/*.tsx"}""";

    private string _complexInput = """
        {
          "file_path": "/src/components/UserProfile.tsx",
          "content": "import React from 'react';\n\nexport const UserProfile = ({ name, email }) => (\n  <div className=\"user-profile\">\n    <h2>{name}</h2>\n    <p>{email}</p>\n  </div>\n);"
        }
        """;

    // ToolResultBlock
    private string _fileContent = """
        export const login = async (email: string, password: string) => {
          const response = await fetch('/api/auth/login', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ email, password })
          });
          return response.json();
        };
        """;

    private string _commandOutput = """
        > npm run test -- --coverage

        PASS  src/components/Button.test.tsx
        PASS  src/utils/format.test.ts
        PASS  src/services/auth.test.ts

        Test Suites: 3 passed, 3 total
        Tests:       12 passed, 12 total
        Coverage:    87.5%
        """;

    private string _errorOutput = """
        error: ENOENT: no such file or directory, open '/src/missing-file.ts'
        at Object.openSync (node:fs:603:3)
        at Object.readFileSync (node:fs:471:35)
        """;

    private string _searchResults = """
        src/components/Login.tsx:15:    const handleSubmit = async (e: FormEvent) => {
        src/components/Login.tsx:23:    await login(email, password);
        src/services/auth.ts:8:         export const login = async (email: string, password: string) => {
        src/hooks/useAuth.ts:12:        const { login, logout } = useContext(AuthContext);
        """;

    // SessionHistoryList
    private List<MockSession> _sessions = new()
    {
        new MockSession
        {
            SessionId = "session-1",
            Mode = SessionMode.Build,
            LastActivityAt = DateTime.Now.AddHours(-2),
            MessageCount = 15
        },
        new MockSession
        {
            SessionId = "session-2",
            Mode = SessionMode.Plan,
            LastActivityAt = DateTime.Now.AddDays(-1),
            MessageCount = 8
        },
        new MockSession
        {
            SessionId = "session-3",
            Mode = SessionMode.Build,
            LastActivityAt = DateTime.Now.AddDays(-3),
            MessageCount = 42
        }
    };

    public class MockSession
    {
        public string SessionId { get; set; } = "";
        public SessionMode? Mode { get; set; }
        public DateTime LastActivityAt { get; set; }
        public int? MessageCount { get; set; }
    }
}
