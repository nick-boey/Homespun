@inject HttpProjectApiService ProjectApi

<div class="p-2">
    <nav class="flex flex-col">
        <div class="nav-item text-sm">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Projects
            </NavLink>
        </div>
        @if (_projects.Any())
        {
            <div class="project-list">
                @foreach (var project in _projects)
                {
                    <div class="nav-item nav-item-sub text-sm">
                        <NavLink class="nav-link" href="@($"projects/{project.Id}")">
                            @project.Name
                        </NavLink>
                    </div>
                }
            </div>
        }
        <div class="nav-item text-sm">
            <NavLink class="nav-link" href="sessions">
                <span class="bi bi-chat-dots-nav-menu" aria-hidden="true"></span> Sessions
            </NavLink>
        </div>
        <div class="nav-item text-sm">
            <NavLink class="nav-link" href="containers">
                <span class="bi bi-box-nav-menu" aria-hidden="true"></span> Containers
            </NavLink>
        </div>
        <div class="nav-item text-sm">
            <NavLink class="nav-link" href="agents">
                <span class="bi bi-robot-nav-menu" aria-hidden="true"></span> Agents
            </NavLink>
        </div>
        <div class="nav-item text-sm">
            <NavLink class="nav-link" href="settings">
                <span class="bi bi-gear-nav-menu" aria-hidden="true"></span> Settings
            </NavLink>
        </div>
        @if (_isMockMode)
        {
            <div class="nav-item text-sm mt-2 pt-2" style="border-top: 1px solid var(--border-color);">
                <NavLink class="nav-link" href="design">
                    <span class="bi bi-palette-nav-menu" aria-hidden="true"></span> Design
                </NavLink>
            </div>
        }
    </nav>
</div>

@code {
    private bool _isMockMode;
    private List<Project> _projects = [];

    protected override async Task OnInitializedAsync()
    {
        // TODO: Detect mock mode via configuration or API endpoint
        _isMockMode = false;

        _projects = await ProjectApi.GetAllProjectsAsync();
    }
}
