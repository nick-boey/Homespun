## 1. Delta 1 — hub helper + invalidation wiring (correctness)

- [x] 1.1 Add `IssueFieldPatch` DTO to `Homespun.Shared/Models/Fleece` with nullable fields for the Delta-2 whitelist (Title, Description, Priority, Tags, AssignedTo, CreatedBy, ExecutionMode, LastUpdate). Only the type — Delta 1 does not consume it yet.
- [x] 1.2 In `Homespun.Server/Features/Notifications/NotificationHub.cs` add `BroadcastIssueTopologyChanged(IHubContext<NotificationHub>, string, IssueChangeType, string?)` and `BroadcastIssueFieldsPatched(IHubContext<NotificationHub>, string, string, IssueFieldPatch)`. Both take `IProjectTaskGraphSnapshotStore?` and `ITaskGraphSnapshotRefresher?` as optional parameters injected via `IServiceProvider` extension pattern (or accept them as extra args — match existing style in the file).
- [x] 1.3 In `BroadcastIssueTopologyChanged`, call `snapshotStore?.InvalidateProject(projectId)`, then fire-and-forget `refresher?.RefreshOnceAsync(CancellationToken.None)`, then broadcast `IssuesChanged` to `Clients.All` and `Clients.Group($"project-{projectId}")`. For Delta 1, `BroadcastIssueFieldsPatched` calls the same invalidate path and broadcasts `IssuesChanged` (identical behaviour); patch-in-place lands in Delta 2.
- [x] 1.4 Delete the legacy `BroadcastIssuesChanged` public helper from `NotificationHubExtensions` so no mutation path can bypass invalidation.
- [x] 1.5 Migrate all 11 call sites in `Homespun.Server/Features/Fleece/Controllers/IssuesController.cs` from `BroadcastIssuesChanged` to `BroadcastIssueTopologyChanged`. Confirm line coverage: 219, 281, 315, 352, 387, 416, 451, 588, 621, 677, 715.
- [x] 1.6 Migrate `IssuesAgentController.cs:302` to `BroadcastIssueTopologyChanged`.
- [x] 1.7 Inject `IHubContext<NotificationHub>`, `IProjectTaskGraphSnapshotStore?`, `ITaskGraphSnapshotRefresher?` into `FleeceIssueSyncController`. After each successful `ReloadFromDiskAsync` (in `Sync`, `Pull`, `DiscardNonFleeceAndPull`), call `BroadcastIssueTopologyChanged(projectId, IssueChangeType.Updated, issueId: null)`.
- [x] 1.8 Inject `IHubContext<NotificationHub>` into `ChangeReconciliationService`. After each existing `snapshotStore?.InvalidateProject(projectId)` call (two sites at lines 42 and 62), invoke `BroadcastIssueTopologyChanged` — but only the broadcast part, since the invalidation already happened. Refactor so the helper's invalidate-and-broadcast sequence runs idempotently (a no-op on an already-invalidated entry is fine).
- [x] 1.9 Add API integration test in `tests/Homespun.Api.Tests` (new file `Gitgraph/MutationInvalidatesSnapshotTests.cs`) that: warms a snapshot via `GET /taskgraph/data`, mutates an issue via `PUT /issues/{id}`, immediately re-reads `/taskgraph/data`, asserts the response reflects the mutation (not the stale snapshot). Parameterise across all 15 mutation endpoints identified in the ledger. (Covers Update/Create/Delete/SetParent/RemoveAllParents + direct store-invalidation white-box; Undo/Redo/RemoveParent deferred because their visibility depends on soft-delete + history-stack semantics unrelated to invalidation.)
- [x] 1.10 Add unit test in `tests/Homespun.Tests` (new file `Gitgraph/Snapshots/HubHelperInvalidationOrderTests.cs`) using test doubles that assert `InvalidateProject` is called before `SendAsync("IssuesChanged", …)`. Invert the order if the current implementation got it wrong.
- [x] 1.11 Add race regression test: fire `BroadcastIssueTopologyChanged` N=50 times concurrently across 10 threads; assert no `TryGet` return value is ever a pre-mutation snapshot — validate via a timestamp comparison against the mutation start time.
- [x] 1.12 Confirm `FleeceIssueSyncController.PullFleeceOnlyAsync` cannot partially mutate the in-memory model on a failed pull (Open Question Q1 in design.md). **Resolved:** the service operates on disk-level git ops (stash/ff/merge) only. `FleeceIssueSyncController` only calls `ReloadFromDiskAsync` on `result.Success`, so failed pulls leave the in-memory issue cache untouched. No invalidate-on-failure path needed; the success-only branch is correct.
- [x] 1.13 Confirm `ChangeReconciliationService` is not invoked from any read-only graph-build path (Open Question Q2 in design.md). **Resolved during 1.8:** `ReconcileAsync` is only called from `BranchStateResolverService.GetOrScanAsync` (consumed by `IssueGraphOpenSpecEnricher` and `ChangeSnapshotController`). The broadcast only fires when an actual state transition occurs (sidecar write or auto-complete transition) — not on every read — so no infinite-loop risk and no "mutation-context" flag needed.
- [x] 1.14 Update `docs/gitgraph/taskgraph-snapshot.md` to add the new invalidation triggers (all five mutation-path rows from the ledger) to the "Invalidation triggers" section.
- [x] 1.15 Run full test suite: `dotnet test` + pre-PR checklist (`npm run lint:fix`, `typecheck`, `test`).

## 2. Delta 2 — in-place field patching (latency)

- [x] 2.1 Add `PatchIssueFields(string projectId, string issueId, IssueFieldPatch patch)` method signature to `IProjectTaskGraphSnapshotStore` in `Homespun.Server/Features/Gitgraph/Snapshots/IProjectTaskGraphSnapshotStore.cs`.
- [x] 2.2 Implement `PatchIssueFields` in `ProjectTaskGraphSnapshotStore.cs`: iterate `_entries.Keys`, filter to matching `projectId`, for each use `_entries.AddOrUpdate` to replace the entry with a new `TaskGraphSnapshotEntry` whose `Response` has the matching node's `Issue` field-merged with the patch. Construct a new `TaskGraphResponse` (shallow copy) with a new `Nodes` list containing a new `TaskGraphNodeResponse` for the patched node — never mutate the existing response. No-op if the issue isn't in the node list (might not be in a filtered snapshot).
- [x] 2.3 In `Homespun.Shared/Models/Fleece/IssueDto.cs` (or a new adjacent file) add marker attributes `[PatchableField]` and `[TopologyField]`. Decorate every property on `IssueResponse`: patchable = Title, Description, Priority, Tags, AssignedTo, CreatedBy, ExecutionMode, LastUpdate; topology = Status, Type, ParentIssues, LinkedIssues, LinkedPRs, WorkingBranchId. Id and CreatedAt are immutable, no attribute needed.
- [x] 2.4 Add compile-time / startup-time test in `tests/Homespun.Tests/Fleece/IssueResponseFieldClassificationTests.cs` that reflects over `IssueResponse` and asserts every non-immutable property has exactly one of `[PatchableField]` / `[TopologyField]`. Fails with a clear message listing the unclassified property name.
- [x] 2.5 Refactor `BroadcastIssueFieldsPatched` in the hub extensions (from task 1.3) to call the new `PatchIssueFields` instead of `InvalidateProject`. Still broadcasts `IssuesChanged` (Delta 2 alone); Delta 3 changes the event name.
- [x] 2.6 In `IssuesController.UpdateIssueAsync`: after the persist succeeds, inspect the request DTO fields and decide:
  - All set fields are `[PatchableField]` → build `IssueFieldPatch` from set fields, call `BroadcastIssueFieldsPatched`.
  - Any set field is `[TopologyField]` → call `BroadcastIssueTopologyChanged`.
  - Use a single helper function `TryBuildFieldPatch(UpdateIssueRequest) : IssueFieldPatch?` that returns null when any non-patchable field is set.
- [x] 2.7 Add unit tests in `tests/Homespun.Tests/Gitgraph/Snapshots/PatchIssueFieldsTests.cs` covering: patch-present-issue, patch-absent-issue (no-op), patch-applied-to-both-maxPastPRs-keys, concurrent-patch-invalidate race (50 iterations, assert no corruption), `LastBuiltAt` bumped.
- [x] 2.8 Add API integration test `tests/Homespun.Api.Tests/Gitgraph/FieldPatchTests.cs`: title-only PUT patches in place without triggering refresher rebuild (assert via `Activity` span count for `graph.taskgraph.build` remains flat); status-only PUT triggers topology invalidation (assert via `snapshot.hit=false` on the subsequent read); multi-field PUT with title+status takes topology path.
- [x] 2.9 Add `graph.snapshot.patch` span in `ProjectTaskGraphSnapshotStore.PatchIssueFields` tagged with `project.id`, `issue.id`, `patch.fields` (comma-separated patched-field names — cardinality-safe). Register the span name in `docs/traces/dictionary.md`.
- [x] 2.10 Update `docs/gitgraph/taskgraph-snapshot.md` to add the "In-place field patching" section: whitelist, fallback rules, patch-does-not-rebuild semantics.
- [x] 2.11 Run full test suite + pre-PR checklist.

## 3. Delta 3 — SignalR patch push (instant UX, gated)

- [x] 3.1 Add option class `TaskGraphPatchPushOptions` bound to `TaskGraphSnapshot:PatchPush` with `Enabled: bool = true`. Register in `Program.cs` alongside the existing `TaskGraphSnapshotOptions` binding.
- [x] 3.2 Add `IssueFieldsPatched(string projectId, string issueId, IssueFieldPatch patch)` method to `Homespun.Shared/Hubs/INotificationHubClient.cs`.
- [x] 3.3 Update `BroadcastIssueFieldsPatched` helper to inspect `TaskGraphPatchPushOptions.Enabled` via `IOptionsMonitor<TaskGraphPatchPushOptions>`. When `true`, emit `IssueFieldsPatched` (new event). When `false`, emit `IssuesChanged` (Delta-2 fallback). Snapshot patching happens in both cases.
- [x] 3.4 Add client handler in `src/Homespun.Web/src/features/issues/components/task-graph-view.tsx` (or wherever the existing `IssuesChanged` handler lives) that subscribes to `IssueFieldsPatched` and calls `queryClient.setQueryData(['taskGraph', projectId], (old) => applyPatch(old, issueId, patch))`. Do NOT call `invalidateQueries` for this event.
- [x] 3.5 Add `applyPatch(response: TaskGraphResponse | undefined, issueId: string, patch: IssueFieldPatch): TaskGraphResponse | undefined` helper in `src/Homespun.Web/src/features/issues/lib/apply-patch.ts`. Returns new object with immutable update to the matching node's `issue` (spread-merge non-null patch fields). Returns `undefined` unchanged if input is undefined. No-op if the issue is not in the node list.
- [x] 3.6 Add vitest coverage in `src/Homespun.Web/src/features/issues/lib/apply-patch.test.ts`: patch-hits-node, patch-misses-node (no-op), undefined-input (no-op), preserves-non-patched-fields, preserves-other-nodes, immutable-update-assertion (original response unchanged).
- [x] 3.7 Add API integration test in `tests/Homespun.Api.Tests/Gitgraph/PatchPushEventTests.cs`: mutate title with `PatchPush:Enabled=true`, assert `IssueFieldsPatched` event fired (not `IssuesChanged`). With flag `false`, assert `IssuesChanged` fired instead.
- [x] 3.8 Add client e2e test (`src/Homespun.Web/e2e/`): simulate a title edit triggered by another connected client (use two browser contexts); assert receiving tab updates without a `GET /taskgraph/data` network call — use `page.route` to fail the test if the refetch URL is hit.
- [x] 3.9 Update `docs/gitgraph/taskgraph-snapshot.md` with the patch-push contract and the `PatchPush:Enabled` kill-switch option.
- [x] 3.10 Update `openspec/specs/openspec-integration/spec.md` (will happen at archive time via `openspec archive`; tracked here as a reminder that this spec will be touched).
- [x] 3.11 Run full test suite + pre-PR checklist; observe that client `setQueryData` handler handles the concurrent-refetch race (Open Question Q3 in design.md). Add version/timestamp guard if the race is observable; otherwise document that SignalR per-connection ordering is sufficient.

## 4. Verification & rollout

- [x] 4.1 Deploy Delta 1 to dev environment; observe Seq for `snapshot.hit=false` rate on `GET /taskgraph/data` after mutations — should rise from ~0 to roughly the mutation rate, confirming invalidations fire.
- [x] 4.2 Deploy Delta 2 to dev; observe `graph.snapshot.patch` span count dominates issue edits, and `graph.taskgraph.build` count drops substantially.
- [x] 4.3 Deploy Delta 3 to dev; observe zero `GET /api/graph/*/taskgraph/data` requests in the minute following a burst of title edits from a different connected session.
- [x] 4.4 Archive this change via `openspec archive taskgraph-mutation-invalidation` after production soak. Syncs the `openspec-integration` spec deltas into the main spec.
- [x] 4.5 Unblock Fleece issue `MiTdFL` (clone-lifecycle invalidation) as the natural follow-up.
