# composer-accepts-mid-stream

Remove the client-side isProcessing gate that blocks the composer while the agent is running. Claude SDK streaming input mode and the worker InputQueue already support mid-run sends end-to-end.
